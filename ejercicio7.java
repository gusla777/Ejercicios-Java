public class ejercicio7 {
    public static void main(String[] args) {
        //Numeros Decimales entre 0.00 y 0.99 periodico Math.random();
        //para quitarle los decimales lo guardas en un int
        // int numero = (int) Math.random() * 10;
        //va a salir cero pq le va a quitar los decimales primero asi q ponemos parentesis
        // int numero2 = (int) (Math.random() * 10);
 
        // intenta hacer un numero aleatorio entre 10 y 20
        int num = (int)(Math.random()*11);
        System.out.println(num +10);
}
}
